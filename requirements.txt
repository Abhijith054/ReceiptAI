# Core (Lightweight for Vercel)
# Local ML removed for Vercel compatibility (500MB limit).
# Extraction will use Regex + Groq LLM fallback.

# API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# QA / LLM
groq>=0.5.0

# Utilities
Pillow>=10.0.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
requests>=2.31.0
pytesseract>=0.3.10
tqdm>=4.65.0

# Testing
pytest>=8.0.0
httpx>=0.27.0
